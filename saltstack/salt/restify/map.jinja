{% set map = salt['grains.filter_by']({
    'default': {
        'user': 'vagrant',
        'group': 'vagrant',
        'folder':  '/home/vagrant/restify'
    },
}, merge=salt['pillar.get']('restify:lookup')) %}
